Prepare the Church OS app shell for Pre-Sprint 1 handoff and first GitHub main branch push.

Context:
The basic Church OS shell has already been started. Login and placeholder dashboards may exist. This task is NOT to build new product features yet.

Goal:
Harden, organize, and stabilize the app shell so it is ready to be pushed to GitHub as the first clean main branch version. This should become the foundation the rest of the team builds from.

Do NOT build:
- real member CRUD
- household CRUD
- attendance logic
- Sunday check-in logic
- Stripe/giving
- reporting
- AI features
- SMS/email
- production dashboards

Focus only on:
- stability
- structure
- login reliability
- routing reliability
- placeholder pages
- documentation
- GitHub readiness

---

STEP 1 — Verify app starts cleanly

Check:
- frontend starts without errors
- backend starts without errors
- database connects
- no console-breaking errors
- no missing imports
- no broken routes

Fix only shell-level issues.

---

STEP 2 — Confirm authentication baseline

Verify:
- admin login works
- member login works
- invalid login shows a clear error
- logout works
- refresh keeps user session if token exists
- protected routes redirect correctly

Test credentials:
Admin:
admin@churchos.test / Admin123!

Member:
member@churchos.test / Member123!

---

STEP 3 — Confirm role-based routing

Expected behavior:
- Admin login redirects to /admin
- Member login redirects to /member
- Unauthenticated user goes to /login
- Member cannot access admin dashboard
- Admin cannot accidentally land on member dashboard unless intended

Add or fix:
- ProtectedRoute component
- RoleRoute component if needed
- Unauthorized page if needed

---

STEP 4 — Standardize app layout

Create or clean up reusable layout components:

client/src/components/
- AdminLayout.jsx
- MemberLayout.jsx
- Sidebar.jsx
- Topbar.jsx
- DisabledNavItem.jsx
- StatCard.jsx
- EmptyState.jsx
- ComingSoonPage.jsx
- LoadingSpinner.jsx
- ErrorMessage.jsx

Admin layout should show:
- sidebar
- topbar
- logout button
- active dashboard link
- greyed-out future modules

Member layout should show:
- simple navigation
- logout button
- greyed-out future modules

---

STEP 5 — Create placeholder routes/pages

Create stable placeholder pages for future modules.

Admin placeholder pages:
- /admin/members
- /admin/households
- /admin/services
- /admin/attendance
- /admin/check-in
- /admin/giving
- /admin/reports
- /admin/settings

Member placeholder pages:
- /member/profile
- /member/household
- /member/give
- /member/services
- /member/settings

Each placeholder page should:
- render without errors
- show “Coming Soon”
- explain this module is planned for a future sprint
- not contain real backend logic yet

---

STEP 6 — Grey out unfinished functionality

All unfinished navigation items should:
- appear visually disabled or muted
- show a “Coming Soon” badge
- not crash when clicked
- either open a placeholder page or remain disabled consistently

Do not leave broken links.

---

STEP 7 — Add backend health checks

Add backend routes:

GET /api/health
Returns:
{
  "status": "ok",
  "service": "Church OS API"
}

GET /api/db-health
Returns:
{
  "status": "ok",
  "database": "connected"
}

If database fails, return a clear error response.

---

STEP 8 — Standardize backend folder structure

Ensure backend structure is clean:

server/src/
- app.js
- index.js
- routes/
- controllers/
- services/
- middleware/
- db/
- utils/

Make sure existing routes/controllers are in the correct folders.

Do not leave major backend logic directly in index.js unless unavoidable.

---

STEP 9 — Confirm database baseline

Make sure the minimum tables exist:
- churches
- users

Make sure users include:
- id
- church_id
- email
- password_hash
- first_name
- last_name
- role
- is_active
- created_at
- updated_at

Make sure there is a repeatable seed script that creates:
- CJC International church
- admin demo user
- member demo user

Add a reset/reseed command if possible.

---

STEP 10 — Add shared constants

Create shared files for future consistency:

shared/roles.js
- admin
- member

shared/routes.js
- admin route names
- member route names

shared/permissions.js
- simple permission map placeholder

Do not overbuild permissions yet. Just create a clean starting point.

---

STEP 11 — Add environment files

Create or update:

.env.example

Include:
DATABASE_URL=
JWT_SECRET=
PORT=
NODE_ENV=
VITE_API_URL=

Make sure real .env files are ignored by Git.

---

STEP 12 — Update .gitignore

Make sure .gitignore includes:
node_modules
.env
.env.local
.DS_Store
dist
build
coverage
logs
*.log

Do not commit secrets.

---

STEP 13 — Add README for team handoff

Update README.md with:

Project:
Church OS

Current milestone:
Pre-Sprint 1 app shell

What currently works:
- admin login
- member login
- role-based routing
- protected dashboards
- placeholder navigation
- health checks

What is intentionally not built yet:
- members module
- households module
- attendance
- check-in
- giving
- reports

Setup instructions:
- install dependencies
- configure .env
- run database schema
- seed demo users
- start backend
- start frontend

Test credentials:
Admin:
admin@churchos.test / Admin123!

Member:
member@churchos.test / Member123!

GitHub instructions:
- this version is intended to be pushed as the first stable main branch
- future work should branch from dev
- do not commit directly to main after this initial shell

---

STEP 14 — Add initial developer handoff notes

Create:

docs/developer-handoff.md

Include:
- folder structure explanation
- current routes
- auth flow explanation
- test login flow
- known placeholders
- next sprint modules
- domain ownership:
  - Dev 1: Auth + Roles
  - Dev 2: Members + Households
  - Dev 3: Services + Attendance + Check-In
  - Lead/PM: Giving + Dashboard
  - Dev 4: Website

---

STEP 15 — Final smoke test

Before declaring complete, run this checklist:

Frontend:
- Login page loads
- Admin demo login works
- Member demo login works
- Logout works
- Refresh works
- Placeholder pages render

Backend:
- /api/health works
- /api/db-health works
- login endpoint works
- invalid login returns error

Database:
- seeded users exist
- password hashing works
- users linked to church

GitHub readiness:
- no secrets in repo
- README updated
- .env.example exists
- app runs cleanly
- no broken imports
- no broken routes

---

GitHub Push Instructions:
After completing fixes, prepare this project to be pushed to GitHub as the first stable main branch.

Do not actually push if credentials are needed from me, but provide the exact commands I should run:

git status
git add .
git commit -m "Initial Church OS app shell"
git branch -M main
git remote add origin [REPO_URL]
git push -u origin main

Also provide the next recommended commands to create dev and staging branches:

git checkout -b dev
git push -u origin dev

git checkout main
git checkout -b staging
git push -u origin staging

Final output:
- Summarize what was cleaned up
- Confirm what works
- Confirm what remains intentionally placeholder
- Provide GitHub push commands