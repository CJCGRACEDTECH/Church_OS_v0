Fix the Church OS app shell login issue.

Current problem:
When I enter credentials and press “Sign In,” nothing changes. I am not redirected to the admin/member dashboard, and I do not see the greyed-out placeholder functionality/navigation.

Important:
Do NOT rebuild the whole app.
Do NOT add new major features.
Do NOT implement Stripe, attendance, check-in, households, or giving functionality.
Only debug and fix the login flow, auth state, role-based redirect, protected routes, and dashboard placeholder visibility.

Goal:
After this fix, I should be able to:
1. Open the login page
2. Click “Use Admin Demo” or enter admin credentials
3. Press “Sign In”
4. Be redirected to the Admin Dashboard
5. See disabled/greyed-out admin navigation items with “Coming Soon” labels
6. Logout successfully
7. Click “Use Member Demo” or enter member credentials
8. Press “Sign In”
9. Be redirected to the Member Dashboard
10. See disabled/greyed-out member navigation items with “Coming Soon” labels

Test Credentials:

Admin:
email: admin@churchos.test
password: Admin123!

Member:
email: member@churchos.test
password: Member123!

Please debug in small steps.

Step 1 — Check the Login Button
- Confirm the Sign In button has an onClick or form onSubmit handler.
- Confirm the form submission is not blocked silently.
- Add console logs temporarily to confirm the login function is firing.
- Prevent default form submission if needed.

Step 2 — Check Frontend Auth Service
- Confirm the login page calls the correct backend endpoint.
- Confirm the endpoint path matches the backend route exactly.
- Expected endpoint:
  POST /api/auth/login
- Confirm email and password are being sent in the request body.
- Confirm errors are displayed on the login page if login fails.

Step 3 — Check Backend Login Route
- Confirm Express has the route:
  POST /api/auth/login
- Confirm the route receives email and password.
- Confirm the route checks the seeded users.
- Confirm password comparison works.
- Confirm the response returns:
  - token or session value
  - user id
  - email
  - first_name
  - last_name
  - role
  - church_id

Expected response shape:
{
  "token": "...",
  "user": {
    "id": "...",
    "church_id": "...",
    "email": "admin@churchos.test",
    "first_name": "Admin",
    "last_name": "User",
    "role": "admin"
  }
}

Step 4 — Check Seed Users
- Confirm both test users exist.
- Confirm passwords were hashed correctly.
- Confirm the login password comparison works for:
  - Admin123!
  - Member123!
- If seed data is missing or wrong, fix the seed script.
- Do not store plain text passwords in the database.

Step 5 — Check Token/Auth State Storage
- Confirm successful login stores the token and user object.
- Use localStorage for now if that is the simplest stable option.
- Store:
  - churchos_token
  - churchos_user
- Confirm the auth state updates immediately after successful login.

Step 6 — Check Role-Based Redirect
After successful login:
- If user.role === "admin", navigate to /admin
- If user.role === "member", navigate to /member

Make sure routing paths match the actual routes in App.jsx.

Step 7 — Check Protected Routes
- If no token exists, redirect to /login
- If admin route is accessed by member, redirect to /member
- If member route is accessed by admin, redirect to /admin or allow only if intended
- Avoid infinite redirect loops

Step 8 — Check Dashboards
Confirm these routes render correctly:
- /admin → AdminDashboard
- /member → MemberDashboard

Admin Dashboard must show:
- welcome message
- dashboard cards
- greyed-out navigation items:
  - Members
  - Households
  - Services
  - Attendance
  - Sunday Check-In
  - Giving
  - Reports
  - Settings
Each disabled item should show “Coming Soon”

Member Dashboard must show:
- welcome message
- placeholder cards
- greyed-out navigation items:
  - Profile
  - Household
  - Give
  - Services
  - Settings
Each disabled item should show “Coming Soon”

Step 9 — Add Visible Error/Loading States
- Show “Signing in...” while login request is processing
- Show a readable error if credentials fail
- Show backend connection error if API call fails
- Do not fail silently

Step 10 — Add Simple Debug Checklist to README
Update README with:
- how to run frontend
- how to run backend
- how to run seed script
- test login credentials
- expected login behavior
- troubleshooting notes if login does not work

Acceptance Criteria:
- Pressing Sign In triggers the login function
- Invalid credentials show error message
- Admin credentials redirect to /admin
- Member credentials redirect to /member
- Dashboards render after login
- Greyed-out coming-soon functionality appears
- Logout clears localStorage/session
- Refreshing the page keeps the user logged in if token exists
- App does not crash
- No unrelated major features are added

Important final instruction:
Fix the existing login flow and routing only. Keep the app shell minimal and stable.